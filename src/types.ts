
export interface Entrenamiento {
    id?: string;      
    fecha: string;    // Fecha en formato ISO (ej. "2025-04-06")
    duracion: number; // Duración en minutos
    distancia: number;// Distancia en kilómetros
  }
  